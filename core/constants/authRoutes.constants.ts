export const authRoutes = ['/login', '/signup'];

export const rootRoute = '/';

export const apiAuthPrefix = '/api/auth';

export const LOGIN_REDIRECT = '/home';

export const NOT_LOGGED_IN_REDIRECT = '/login';
